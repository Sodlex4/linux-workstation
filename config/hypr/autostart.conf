# Wallpaper daemon (awww, replaces swaybg)
exec-once = uwsm-app -- awww-daemon
# Wallpaper slideshow — cycles every 10 minutes with fade transitions
exec-once = uwsm-app -- $HOME/.local/bin/omarchy-bg-slideshow
# Extra autostart processes
# exec-once = uwsm-app -- my-service
